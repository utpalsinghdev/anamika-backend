name: Green Apple Backend CI/CD

on:
  pull_request:
    branches: [development]

jobs:
  checkdir:
    runs-on: self-hosted
    steps:
      - name: Full Checkout
        run: cd /root/green-apple-backend && git pull origin development

      - name: Install Dependencies
        run: cd /root/green-apple-backend && npm install && npm run build && npm run format && pm2 list && pm2 ls

      - name: Making Build Directory
        run: cd /root/green-apple-backend && mkdir building
        
      

      - run: ls
      - run: npm install
      - run: npm run format
      - run: npm run build --if-present
